# Requirements for building and obfuscation
# Install: pip install -r requirements_build.txt

# For obfuscation (built-in, no extra needed)
# base64, zlib, ast, re - all built-in

# For Nuitka build (recommended)
nuitka>=1.8.0

# For Cython build (alternative)
cython>=3.0.0

# For PyInstaller (if using Cython method)
pyinstaller>=5.13.0

# Existing dependencies
PyQt6>=6.5.0
requests>=2.31.0
urllib3>=2.0.0

